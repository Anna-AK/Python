# у пользователя запрашивается количество билетов, которые он хочет приобрести на мероприятие.
count_ticets = int(input("Hello! \nPlease enter number of tickets: "))
total = 0;
i = 1
while i <= count_ticets :
    age = int(input(f"Enter age of {i} client: "))
#  Если посетителю конференции от 18 до 25 лет стоимость билета 990 руб.
    if 18 <= age <= 25:
        total += 990
#  Если посетителю конференции более 25 лет — полная стоимость 1390 руб.
    elif age > 25:
        total+= 1390
# Если посетителю менее 18 лет - билет бесплатный
    i += 1

# Если человек регистрирует больше трёх человек на конференцию, то дополнительно получает 10% скидку на полную стоимость заказа.
if count_ticets >= 3:
    total *= 0.9

# В результате программы выводится сумма к оплате.
print("Total ticket price: ", total)
