per_cent = {'ТКБ': 5.6, 'СКБ': 5.9, 'ВТБ': 4.28, 'СБЕР': 4.0}

money = float(input("Введите сумму депозита:"))/100  #получаем размер вклада; переводим в число с плавающей запятой; делим на 100, чтобы иметь значение 1%

deposit = [money*per_cent['ТКБ'],money*per_cent['СКБ'],money*per_cent['ВТБ'],money*per_cent['СБЕР']]  #формируем список получившихся %%

print(list(map(round, deposit)))  #выводим получившийся список процентов по банкам, округляя до целого
print("Максимальная сумма, которую вы можете заработать — ", round(max(deposit)))  #выводим максимальный результат
